/**
 * Punto de entrada del backend.
 *
 * Este archivo es un placeholder del scaffolding (#1). El servidor Express real
 * —config tipada, middlewares, manejo de errores y endpoints— se implementa en
 * el issue #3.
 */

const port = Number(process.env.PORT ?? 3000);

console.log(`[backend] scaffold OK · puerto configurado: ${port}`);
console.log('[backend] el servidor Express se implementa en el issue #3');
